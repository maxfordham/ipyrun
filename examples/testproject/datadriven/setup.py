from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='template folder structure for managing and processing data from various different input streams in a self contained folder.',
    author='j.gunstone',
    license='',
)
